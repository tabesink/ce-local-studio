[project]
name = "context-engine"
version = "0.1.0"
description = "Context Engine trusted application foundation"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "python-multipart>=0.0.9",
    "sqlalchemy>=2.0.30",
    "alembic>=1.13.2",
    "argon2-cffi>=23.1.0",
    "cryptography>=42.0.0",
    "psycopg[binary]>=3.2.0",
]

[project.optional-dependencies]
lightrag-runtime = [
    "numpy>=1.26.0",
    "json-repair>=0.29.0",
    "python-dotenv>=1.0.0",
    "networkx>=3.2.0",
    "nano-vectordb>=0.0.4",
    "tiktoken>=0.7.0",
    "httpx>=0.27.0",
    "aiohttp>=3.9.0",
]
object-store = [
    "boto3>=1.34.0",
]
parsers = [
    "docling>=2.0.0",
    "reductoai>=0.1.0",
    "httpx>=0.27.0",
]
synthesis = [
    "openai>=1.40.0",
]
# Private LightRAG runtime embedding bindings (not browser/BFF).
# OpenAI is the first packaged embedding provider; Bedrock/Ollama stay
# fail-closed until U7 catalog + smoke prove them.
embeddings = [
    "openai>=1.40.0",
]
# Private governed-preview renderer (DOCX text extraction). Markdown/text/PDF
# paths need no extra; LibreOffice-class packaging remains P12-06 if elevated.
preview-renderer = [
    "python-docx>=1.1.0",
]
test = [
    "pytest>=8.2.0",
    "httpx>=0.27.0",
    "ruff>=0.5.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q --basetemp=tests/.pytest-tmp"
markers = [
    "integration_docker: requires a live Docker daemon; used for Slice A controller lifecycle proof",
    "postgresql: requires explicit opt-in and an administrative PostgreSQL 16 test URL",
]

[tool.setuptools.packages.find]
where = [".", "vendor"]
include = ["context_engine*", "lightrag*"]
