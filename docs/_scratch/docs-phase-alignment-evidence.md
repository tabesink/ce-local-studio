# Documentation Phase Alignment — D0 Evidence and Post-D0 Revalidation

Date: 2026-07-23  
Scope: documentation authority convergence, deterministic phase-scope enforcement, and current brownfield chronology  
Conclusion: D0 and its post-D0 authority revalidation pass. The application remains release-blocked while P0 is in progress and B0/later boundary evidence is incomplete.

## Verified authority outcome

- Phase 1 is the grounded RAG workstation with source/evidence/template governed context and the minimum private operational-safety baseline.
- Product-facing observability is a non-normative Phase 2 direction under `docs/future/`.
- Governed knowledge publication is a non-normative Phase 3 direction under `docs/future/`.
- Active routes are `/login`, `/chat`, `/documents`, `/database-visualize` in its deliberate unavailable state, and `/settings`.
- The PRD owns the sole closed member-chat capability manifest; root guidance, frontend microcopy, the master tracker, and plan 002 link to it without redefining it.
- Plans 002 and 003 remain subordinate Phase 1 child contracts. Evidence suggestions and the Settings Domain accordion remain contract-gated.
- The 2026-07-22 drift review remains immutable historical evidence and is dispositioned through exactly one primary row for every DRIFT-01 through DRIFT-33, each with a governing requirement/case and boundary proof.
- The frontend contract retains the compact dark/light workstation, 320 CSS-pixel and zoom coverage, canonical `src/ui` destination, four unblocked parity targets, and the contract-blocked Settings Domain accordion.
- The clean-install schema does not authorize destructive contraction of a populated legacy database. `docs/architecture/legacy-persistence-retirement.md` and P12-01 retain the compatibility, catalog-reconciliation, backup/key-custody, restore, and refusal gates.

## U6 proof-first evidence

The adversarial fixture suite was expanded before checker behavior changed. Its first run failed as required because a malformed removal-evidence marker with invalid spacing was silently accepted:

```text
fixture unexpectedly passed: malformed-marker-spacing
```

The checker was then hardened to enforce strict manifest table state and cells, lowercase stable slugs, normalized paths, closed non-active classifications, exact mixed-removal marker scope (including inline-code and multiline malformed records while exempting the parent grammar example), all closed prohibited lexemes, sole PRD capability ownership, retained safety anchors, child-plan authority ceilings, required future links, ignore-independent plan discovery, and stable evidence-excluding hash inputs.

The completed suite covers active/future/historical allowance and rejection; every prohibited surface literal; valid, malformed, multiline, duplicate, unknown, extra-content, active-file, and surrounding-prose marker cases; malformed headers/cells/pipes/HTML/continuations; duplicate/unsorted keys; traversal and case aliases; exact-set/cardinality/review-required failures; hidden, ignored, JSON, YAML, extensionless, and ordinary unclassified documents; safety-capability erosion; child positive-authority declarations; and broken/missing future links.

The final manual review added nested-marker, inline-code, and multiline regression fixtures. It reproduced silent passes for nested and inline/multiline records, then passed after marker-like detection was extended without treating the parent plan’s backticked grammar example as a live record. The adversarial fixture suite is now called by `scripts/verify.sh`, and the Phase 1 route test also positively asserts `/health/live` and `/health/ready`.

## P0-02 governance-adoption evidence

The root guidance and documentation entrypoint already carried the intended authority hierarchy, product vocabulary, coding-agent workflow, and explicit-decision stop conditions. P0-02 therefore used characterization-first adoption rather than rewriting settled policy. The first adversarial run failed as expected because replacing the highest-precedence source in `docs/README.md` was silently accepted:

```text
fixture unexpectedly passed: governance-precedence
```

The phase-scope checker now requires the same ordered seven-level authority hierarchy in `AGENTS.md` and `docs/README.md`; the canonical Knowledge Domain, Source Document, Evidence, Conversation, and composer-reference meanings; the prohibition on a Workspace entity; the docs-first and dependency-order agent rules; the rule that reviewed code is evidence rather than completion proof; the missing-contract stop rule; and all six explicit-decision stop conditions. Each assertion is bound to its unique owning Markdown section, with exact line and list-cardinality checks where applicable. Fixtures independently weaken precedence, vocabulary, the Workspace prohibition, the stop-condition heading, a concrete stop condition, and the missing-authority rule while planting the original canonical text as an out-of-section decoy. All decoy cases are rejected with stable diagnostics.

This is task-owned proof for the bounded P0-02 governance deliverable. It does not complete P0-03 contracts, P0-04 ownership/state conventions, P0-06 generation parity, or P0-07 deferred-runtime removal.

## Deterministic verification

The following commands passed from the repository root:

```text
bash -n scripts/check-doc-phase-scope.sh
bash -n scripts/tests/check-doc-phase-scope.sh
bash scripts/tests/check-doc-phase-scope.sh
  phase-scope fixtures: PASS
bash scripts/check-doc-phase-scope.sh
  phase-scope: PASS (54 governed files)
```

The fixed-point procedure hashed every path returned by `scripts/check-doc-phase-scope.sh --print-inputs` before and after syntax, fixture, and live scans and compared the inventories byte-for-byte. Result: PASS.

- Authority inputs hashed: 56
- Combined sorted authority-input SHA-256: `4de613d2179338ae95240f2634f8ccb72c603ae877bf1c51a354b84e0b94c69a`
- Phase manifest: `docs/phase-scope-manifest.md`
- Brownfield register: `docs/brownfield-refactor-register.md`
- Checker: `scripts/check-doc-phase-scope.sh`
- Fixture suite: `scripts/tests/check-doc-phase-scope.sh`
- Evidence output: excluded from `--print-inputs` by contract

## Application chronology and integrity

The original D0 preflight digest remains immutable:

```text
efeae0f3d8e755c96e1829de2edb7f210da28acbb0e69dd50ecdef2261195d78  -
```

That digest matched at the original D0 run, proving no application file changed during the original documentation convergence. Post-D0 application refactoring then intentionally changed the application tree. Those later changes include the canonical `app/context_engine` layout, a closed error-record slice, the root verification skeleton, removal of public deferred-route registration and browser affordances, Phase 1-only composer discovery/validation/persistence/prompt assembly, focused legacy-Wiki rejection coverage, initial OpenAPI/TypeScript generation artifacts, the live/adversarial generated-contract gate plus first generated browser request type, and repair of the inherited frontend type/build and stale lifted documentation-test failures.

Because those mutations occurred after original D0—and some landed while this revalidation was inspecting current state—the current tree must not be compared to the original baseline as if it were still the D0 tree. The tracker was reopened and updated rather than hiding the chronology.

The latest post-D0 checkpoint captured the current application digest after the complete syntax, fixture, live-scan, focused backend, and root verification sequence:

```text
583270776dc86176c152a30a191c1cf4946ff395f539cf70894077e849116238  -
```

Result: recorded current-tree checkpoint (generated `.pytest-tmp` content excluded); the documentation gates, 40 backend tests, generated OpenAPI/TypeScript live and stale-artifact checks, 53 frontend tests, frontend typecheck/build, backend image build, Compose configuration, and the complete current root loop passed. The original baseline file was read only; its own file SHA-256 was `7c5810115ab15501f9d2f05aa1b191db5cc6708a936dff91e9130acd88019143`.

## Current brownfield checkpoint

- P0 is `IN_PROGRESS`.
- P0-01 is `DONE` only because `docs/_scratch/p0-01-layout-inventory.md` records its bounded proof.
- P0-02 is `DONE` because the live and adversarial documentation gate now pins its authority, vocabulary, coding-agent, and stop-condition boundary.
- P0-05 is `DONE` for its bounded CI deliverable. P0-03, P0-06, and P0-07 remain `IN_PROGRESS`; each has partial current-tree evidence and named remaining gaps.
- Generated OpenAPI and TypeScript artifacts now reproduce byte-for-byte under live and adversarial stale-artifact gates, production and generation share route registration, inherited artifact-path overrides cannot bypass the production comparison, health success/readiness-failure responses use closed generated components, and the closed/bounded login request is pinned through the registered operation to the generated browser capability call. Most capability response DTOs and all SSE shapes remain handwritten; canonical HTTP catalog parity and SSE schema/fixture generation are not complete.
- Public deferred-route registration and browser affordances are absent; the active backend package, ORM metadata, accepted-ref schema target, and audit vocabulary contain no Wiki implementation; composer paths admit only source/evidence/template refs and reject legacy Wiki tokens/content. The audit/diagnostic browser service, read-event vocabulary, diagnostic-only metadata, and stale frontend observability expectations are also absent while private audit recording and health/readiness remain positively exercised. Fresh-install and populated-upgrade proof remain blocked because the configured Alembic directory/history is missing.
- No phase, B0, production release, populated-database compatibility path, or DRIFT row is marked complete by presence of lifted code.

## Current repository gate

The latest `scripts/verify.sh` run passed every current check: documentation scope and fixtures, Python lock integrity, backend import/lint, all 40 backend tests, frontend dependency installation, generated OpenAPI/TypeScript live comparison and adversarial stale-artifact fixtures, frontend typecheck, all 53 frontend tests, the production frontend build, the backend Docker build, and Compose configuration. The frontend repair preserves the feature-owned delete-button layout without widening the legacy `SettingsButton` API, adds an exact App Router page inventory, and replaces lifted app-relative UIUX-factory expectations with assertions against root `DESIGN.md`, the reviewed `docs/frontend/` authority, and the subordinate phase-child plan. The production build exposes only the Phase 1 route set. `npm ci` still reports six high-severity dependency advisories, and Starlette reports its current TestClient/httpx compatibility path as deprecated, Node reports module-type warnings, and Next reports the middleware convention as deprecated; these are recorded evidence, not authorization for forced dependency or framework migration. The green local loop still lacks HTTP catalog/response-model parity, canonical SSE schemas/fixtures, real PostgreSQL/migration behavior, privacy scans, browser E2E, and deployed-ingress integration, so P0-06, B0, and release readiness remain incomplete.

## Non-release boundary

This evidence validates documentation structure, phase authority, traceability, exact negative declarations, retained safety anchors, child-plan ceilings, fixed-point inputs, and honest application chronology. It does not prove migrations, complete runtime removal, generated-client adoption, PostgreSQL concurrency, native adapters, browser behavior, runnable full Compose startup, deployed-ingress streaming, backup/restore, or production readiness. Those remain incomplete or explicitly blocked in `docs/master-build-plan.md`.
