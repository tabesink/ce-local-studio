# Known Residuals — P2-01 review

Source: post-implementation review of `e187555` (2026-07-24)

Verdict: approve-with-nits. Applied: model-profile create audit `target_id` binding.

## Accepted residuals

1. Closed `ProviderSummaryDto` / `ModelProfileDto` / `RuntimeSettingsDto` projection (`version`, `inUse`, `configured`, `displayName`) and `If-Match` / `stale_revision` remain with P2-02.
2. Credential encryption/rotation contract proof (A-01 races, Fernet key management, write-only credential request shapes) remains with P2-02.
3. Immutable embedding-dimension rejection when a domain already references a profile, beyond the current in-use delete fence, remains with P2-03.
4. API lifespan still ensures the closed catalog/defaults on startup (now insert-only); moving that ensure into an explicit release bootstrap remains available to P10.
5. Multi-agent `ce-code-review` dispatch was interrupted in-session; this residual file records the inline defect-first review outcome instead.
